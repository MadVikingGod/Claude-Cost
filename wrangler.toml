# Cloudflare Workers static-assets configuration for this static site.
# Deploy from the repo root with:  npx wrangler deploy
# (This is an assets-only Worker: no server code, it just serves ./public.)
name = "claude-cost"
compatibility_date = "2026-05-31"

[assets]
directory = "./public"
