# Cloudflare Pages configuration for this static site.
# Deploy from the repo root with:  wrangler pages deploy .
name = "claude-cost"
compatibility_date = "2026-05-31"

# The site is plain static files with no build step, so the build output
# directory is the repo root itself.
pages_build_output_dir = "."
